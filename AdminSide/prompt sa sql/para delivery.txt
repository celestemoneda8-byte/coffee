-- Create notifications table and optional helper indexes
CREATE TABLE IF NOT EXISTS `notifications` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `user_id` INT UNSIGNED DEFAULT NULL,         -- NULL = broadcast to all admins
  `type` VARCHAR(50) NOT NULL DEFAULT 'info',  -- eg. 'order_delivered','order_cancelled'
  `title` VARCHAR(191) NOT NULL,
  `message` TEXT,
  `order_id` INT UNSIGNED DEFAULT NULL,
  `is_read` TINYINT(1) NOT NULL DEFAULT 0,
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX (`user_id`),
  INDEX (`order_id`),
  INDEX (`is_read`),
  INDEX (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- Note: If your users table does not have an 'email' column and you want email notifications,
-- add one (uncomment and run if needed; adapt the column type/length as desired):
-- ALTER TABLE users ADD COLUMN IF NOT EXISTS email VARCHAR(191) DEFAULT NULL;